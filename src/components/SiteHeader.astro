---
const links = [
  { href: "/", label: "Home" },
  { href: "/portfolio", label: "Portfolio" },
  { href: "/about", label: "About" },
  { href: "/contact", label: "Contact" },
];
const current = Astro.url.pathname.replace(new RegExp("/+" + "$"), "") || "/";
---
<header class="sticky top-0 z-40 bg-white/80 backdrop-blur border-b">
  <nav class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 h-14 flex items-center justify-between">
    <a href="/" class="font-bold">SMooks</a>
    <ul class="flex gap-5">
      {links.map(l => (
        <li>
          <a
            href={l.href}
            aria-current={current===l.href ? "page" : undefined}
            class={`hover:underline ${current===l.href ? "font-semibold underline" : ""}`}
          >{l.label}</a>
        </li>
      ))}
    </ul>
  </nav>
</header>
